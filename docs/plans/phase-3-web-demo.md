# Keryx Image PoC Phase 3: Minimal Web Demo

Build a local funded-devnet backend and one responsive page that submits an image prompt, tracks request/claim/generation/IPFS status, and displays the resulting image.

Multiple-image generation creates independent requests with derived seeds and pays each completed request separately. Browser-wallet integration and production UI scope are excluded.
